const convertUptime = require('./convertUptime');
const getService = require('./getService');
const getConfig = require('./getConfig');

module.exports = {
  convertUptime,
  getService,
  getConfig,
};
